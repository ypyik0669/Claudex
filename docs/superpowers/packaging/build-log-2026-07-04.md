# 打包记录

## 打包方式

- Windows：GitHub Actions 在 Windows runner 上构建。
- macOS：GitHub Actions 在 macOS runner 上构建。

## 资产规则

只上传 `release/` 顶层的 `.exe`、`.zip`、`.dmg` 文件，不上传内部运行文件。

## 风险

macOS 包暂未签名，第一次打开需要用户手动允许。
