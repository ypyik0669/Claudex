# 构建记录

## 结果

已验证本地构建和桌面 QA。Windows 和 macOS Release 由 GitHub Actions 构建。

## 命令

```bash
npm run build
npx electron qa/capture-pass34-calm-shell.cjs
```

## 说明

本地不提交 `dist/`、`release/` 或打包产物。Release 资产由 CI 上传。
